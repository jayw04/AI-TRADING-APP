# NON-GOVERNING HISTORICAL PROTOTYPE

Contains the rejected moving-block bootstrap.
Must not be imported, executed, or used as an evaluator reference.
Superseded by Increment 1 v1.2 stationary-bootstrap implementation.

---

Context: this directory holds the v0.2-era Validation/OOS metric-primitive prototype
(`mr002_valoos_metrics_prototype.py`) and the session-index extraction output. The prototype's
bootstrap is the **moving-block / L21 / 2000 / seed42** variant that owner Ruling 1 (2026-07-20,
`docs/review/comments.md`) **rejected as transcription drift**. The governing preregistration
`MR002_ValidationOOS_Preregistration_v1.0.4.json` restores the frozen v0.3 **stationary
(Politis–Romano, circular)** bootstrap, and its `reference_implementation` now points at the
qualified evaluator `docs/review/mr002/evaluator/mr002_valoos_metrics.py`. This prototype is retained
only as a historical record of the superseded chain; it is not part of the qualified evaluator and
must not be treated as governing.

`MR002_SessionIndex_Extraction_Output.json` in this directory remains a *governing frozen source*
(registered session-date metadata) and is unaffected by this notice — only the prototype
**implementation** is superseded.
