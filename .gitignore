# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
*.egg-info/
*.egg
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/
coverage.xml
*.cover

# P5 §3: TOTP enrollment QR codes written by scripts/create_user.py. These
# embed the TOTP secret — never commit them.
totp_*.png

# Node
node_modules/
dist/
build/
.pnpm-store/
.next/
.cache/
*.tsbuildinfo

# Env / secrets
.env
.env.*
!.env.example
alpaca info.txt
alpaca_info.txt
Untitled
secrets/

# Local data
*.sqlite
*.sqlite-journal
*.sqlite-shm
*.sqlite-wal
data/
bars_cache/
# Fetched market-data parquets cached by scripts/backtest_range_trader_sweep.py
scripts/.sweep_cache/
apps/backend/scripts/.sweep_cache/
# P10 Phase 3A portfolio study (§4.9): the comparison report (.md) is committed; the
# bulky, fully-reproducible evidence-bundle curves + any local stores are not.
research/**/*.json
research/**/*.duckdb

# Editor / OS
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/settings.json

# Claude Code (commit shared settings.json, ignore personal .local.json)
.claude/settings.local.json
.claude/*.local.json

# Logs
*.log
logs/
certs/
docker-compose.override.yml

# Local-only secrets — login password + per-user TOTP secrets; never commit to git.
**/app login info.txt
/User Information.txt

# Daily operational reports: the generator (scripts/reports/) + README are tracked,
# but the dated outputs are regenerable and contain account numbers — keep them local.
/reports/*.md
!/reports/README.md

# never track build/scratch artifacts under scripts/
apps/backend/scripts/__pycache__/
apps/backend/scripts/.sweep_cache/

# MR-002 research container scratch output (never evidence; evidence is written deliberately)
.mr002out/
