"""SEC-001 V3 classification crawl — V3-owned acquisition driver.

Reads EDGAR under a frozen fair-access policy and emits facts. Modifies no MR-002 file,
computes no coverage quantity, and never writes outside its two declared output prefixes.
"""
