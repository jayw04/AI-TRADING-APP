"""TODO: real auth.

Replace `app.auth.stub.get_current_user` with a real identity layer
(JWT/OIDC/session) before exposing the workbench beyond a single local user.
P0 ships with a single seeded dev user (`id=1`).
"""
